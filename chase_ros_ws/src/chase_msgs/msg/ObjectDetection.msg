# If object is found, can also be implmentet by giving negative or 0 vvalues to pos_arguments
bool object_found

# Position from lower left corner, in pixels
int32 pos_x # [pixels]
int32 pos_y # [pixels]

# Image size
int32 image_x # [pixels]
int32 image_y # [pixels]

# time for observation
time time_stamp